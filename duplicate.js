const names = ['abul', 'babul', 'cabul', 'gabul', 'rabul', 'abul', 'tabul', 'babul', 'habul', 'abul', 'mabul', 'babul'];

function removeDuplicate(names){
    for(let i = 0; i < names.length; i++){
        const element = names[i];
        console.log(element);
    }
}

const uniqueNames = removeDuplicate(names);